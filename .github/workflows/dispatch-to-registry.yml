# Auto-sync the FlutterSDK AI registry when a fluttersdk_telescope release is published.
#
# Fires repository_dispatch at fluttersdk/ai. The registry's sync.yml receives
# the event, pulls the skills/fluttersdk-telescope/ subtree, bumps the version
# triplet, and pushes to main.
#
# Called by publish.yml after its github-release job, so the dispatch happens
# only once pub.dev has accepted the release. It is NOT triggered by
# `release: [published]`, which is what it used to declare: publish.yml creates
# the release with softprops/action-gh-release under the default GITHUB_TOKEN,
# and GitHub does not start workflow runs from events raised by that token. So
# that trigger could never fire. It was never going to be noticed either, since
# the switch to it landed on 2026-08-03 and no release has been cut since
# 0.0.4 (2026-06-17): the run history holds no `release` event at all.
#
# Deliberately NOT on every push. A skill edit that lands on master between
# releases waits for the release that carries it, so the registry version
# tracks published packages instead of counting commits: under the old push
# trigger the registry reached v1.3.75, most of those re-publishing the same
# skill content. Reach for the manual run when a skill fix has to arrive
# before the next release.
#
# Auth: uses the fluttersdk-registry GitHub App (1-hour mint-on-demand
# tokens). Requires REGISTRY_BOT_APP_ID + REGISTRY_BOT_PRIVATE_KEY
# secrets on this repo (same values as on fluttersdk/ai).

name: Dispatch skill update to registry

on:
    workflow_call:
        secrets:
            REGISTRY_BOT_APP_ID:
                required: true
            REGISTRY_BOT_PRIVATE_KEY:
                required: true
    workflow_dispatch:

permissions:
    contents: read

jobs:
    dispatch:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v7
              with:
                  fetch-depth: 1

            - name: Extract version
              id: ver
              run: |
                  set -euo pipefail
                  # `v?` because this repo tags WITHOUT the prefix: publish.yml
                  # matches '[0-9]+.[0-9]+.[0-9]+*' and every release so far is
                  # `0.0.4`-shaped. The old `^v` test could never match a real
                  # tag here, so this always fell through to pubspec.yaml.
                  if [[ -n "${GITHUB_REF_NAME}" && "${GITHUB_REF_NAME}" =~ ^v?[0-9]+\.[0-9]+\.[0-9]+ ]]; then
                      VER="${GITHUB_REF_NAME#v}"
                  else
                      VER=$(grep -E '^version:' pubspec.yaml | head -1 | awk '{print $2}')
                  fi
                  # Strip Dart/Flutter build metadata suffix (e.g. 1.2.3+4 -> 1.2.3)
                  VER="${VER%%+*}"
                  if [[ -z "$VER" ]]; then
                      echo "::error::could not extract a non-empty version from ref or pubspec.yaml"
                      exit 1
                  fi
                  echo "version=$VER" >> "$GITHUB_OUTPUT"

            - name: Mint App token scoped to fluttersdk/ai
              id: app-token
              uses: tibdex/github-app-token@v2.1.0
              with:
                  app_id: ${{ secrets.REGISTRY_BOT_APP_ID }}
                  private_key: ${{ secrets.REGISTRY_BOT_PRIVATE_KEY }}
                  installation_retrieval_mode: repository
                  installation_retrieval_payload: fluttersdk/ai
                  repositories: '["ai"]'

            - name: Fire repository_dispatch to fluttersdk/ai
              uses: peter-evans/repository-dispatch@v4
              with:
                  token: ${{ steps.app-token.outputs.token }}
                  repository: fluttersdk/ai
                  event-type: skill-updated
                  client-payload: |
                      {
                          "source": "telescope",
                          "version": "${{ steps.ver.outputs.version }}",
                          "sha": "${{ github.sha }}"
                      }
